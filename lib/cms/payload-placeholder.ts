// Placeholder file to keep CMS integration concerns grouped in one location.
// Later, Payload config helpers, API clients, and content types can live here.
export type PlaceholderContent = {
  slug: string;
  title: string;
  summary: string;
};
